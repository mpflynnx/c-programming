#include <stdio.h>

int main(void)
{

    // inputs to program
    int speed = 20;
    int time = 7;

    // processing
    int distance = speed * time;

    // output
    printf("distance: %d\n", distance);

}